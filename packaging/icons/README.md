# App Icons

Put your packaging icons here:

- `app.icns` for macOS
- `app.ico` for Windows

You can replace them later without changing any code.
Just keep the filenames the same and rebuild the app.
