{
  "name": "dtm-qb-relay",
  "version": "1.0.0",
  "description": "QuickBooks OAuth redirect relay for DTM Vehicle Builder",
  "private": true,
  "scripts": {
    "test": "node --test test/*.test.mjs"
  },
  "dependencies": {
    "@netlify/blobs": "^10.0.8",
    "jose": "^6.1.0"
  },
  "engines": {
    "node": ">=20"
  }
}
